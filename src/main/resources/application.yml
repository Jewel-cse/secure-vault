spring:
  application:
    name: payment-service
  
  # Spring Cloud Vault Configuration
  config:
    import: vault://
  
  cloud:
    vault:
      # Vault server connection
      uri: http://localhost:8300
      authentication: APPROLE
      
      # AppRole authentication
      app-role:
        role-id: ${VAULT_ROLE_ID:e3563d24-4ee3-cc91-cf8c-559ee98a9b11}
        secret-id: ${VAULT_SECRET_ID:176be433-44d2-e46b-81f4-48e345d67668}
        role: payment-service
        app-role-path: approle
      
      # KV v2 secrets engine configuration
      kv:
        enabled: true
        backend: secret
        default-context: payment-service
        
      # Fail fast if Vault is not available
      fail-fast: false

# Application port
server:
  port: 8080

# Logging
logging:
  level:
    org.springframework.cloud.vault: DEBUG
    org.springframework.vault: DEBUG
